import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const DiscogsAuth = () => {
  return (
    <>
      <View style={styles.container}>
        <Text>DiscogsAuth</Text>
      </View>
    </>
  );
};

const styles = StyleSheet.create({
  container: {
    borderColor: 'red',
    borderWidth: 2,
  },
});

export default DiscogsAuth;
